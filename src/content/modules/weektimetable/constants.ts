export const HOUR_WIDTH = 160        // px per hour
export const SHORT_THRESHOLD = 1.0   // hours — events shorter than this are "short"
export const TOTAL_HOURS = 24
export const TRACK_WIDTH = HOUR_WIDTH * TOTAL_HOURS
export const TIME_AXIS_HEIGHT = 26   // keep in sync with --time-axis-height in index.css

export const MAX_EVENT_TEMPLATES = 100
export const MIN_EVENT_DURATION_MINUTES = 5
